/**
 * @fileOverview design right
 * @date 2023-08-28
 * @author poohlaha
 */
import React, { ReactElement } from 'react'
import { observer } from 'mobx-react-lite'

const DesignRight = (): ReactElement => {
  const render = () => {
    return <div className="design-right"></div>
  }

  return render()
}

export default observer(DesignRight)
